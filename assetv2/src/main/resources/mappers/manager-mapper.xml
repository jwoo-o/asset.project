<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="manager">
  <select id="login" resultType="managerVo">
    	select mNo,mName,mId,empNo,
    	(select cmNm from cmcd c where c.cmCd='division' and c.cmNo=dept) dept,
    	position from manager where mId = #{mId} and pwd = SHA2(#{pwd},256)
  </select>
  
</mapper>